# ---------------------------------------------------------------------------
# Deployment (docker-compose.prod.yml). See DEPLOY.md.
# ---------------------------------------------------------------------------
# Public domain that points (A record) at this server. Caddy obtains a
# Let's Encrypt certificate for it automatically.
DOMAIN=fishingteam.su

APP_ENV=prod
# Shared secret for internal/desktop APIs. MUST be changed from the default.
# Generate: openssl rand -hex 32
INTERNAL_API_TOKEN=dev-internal-token

POSTGRES_DB=grey_cardinal
POSTGRES_USER=grey
# MUST be changed from the default. Generate: openssl rand -hex 24
POSTGRES_PASSWORD=grey
# DATABASE_URL must embed POSTGRES_USER/POSTGRES_PASSWORD above.
DATABASE_URL=postgresql+asyncpg://grey:grey@postgres:5432/grey_cardinal

# Demo flow: auto-confirm desktop transcript proposals into tasks so they appear
# without a manual confirm step. Set false for production.
DESKTOP_AUTO_CONFIRM_PROPOSALS=true

BRAIN_API_HOST=0.0.0.0
BRAIN_API_PORT=8000
BRAIN_API_BASE_URL=http://brain-api:8000

TELEGRAM_BOT_HOST=0.0.0.0
TELEGRAM_BOT_PORT=8010
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
TELEGRAM_PUBLIC_BASE_URL=https://example.ngrok-free.app
TELEGRAM_BOT_BASE_URL=http://telegram-bot:8010

LLM_PROVIDER=openai_compatible
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

BOARD_PROVIDER=mock
YOUGILE_API_BASE_URL=https://ru.yougile.com
YOUGILE_API_KEY=
YOUGILE_COMPANY_ID=
YOUGILE_PROJECT_ID=
YOUGILE_BOARD_ID=
YOUGILE_COLUMN_BACKLOG_ID=
YOUGILE_COLUMN_TODO_ID=
YOUGILE_COLUMN_IN_PROGRESS_ID=
YOUGILE_COLUMN_REVIEW_ID=
YOUGILE_COLUMN_BLOCKED_ID=
YOUGILE_COLUMN_DONE_ID=

REMINDER_DEADLINE_HOURS_BEFORE=2
REMINDER_STALE_HOURS=24
EVENING_DIGEST_HOUR=20
DEFAULT_TIMEZONE=Europe/Moscow
DEFAULT_WORKSPACE_NAME=Hackathon Team
DEFAULT_TELEGRAM_CHAT_ID=

AUDIO_WORKER_HOST=0.0.0.0
AUDIO_WORKER_PORT=8020
AUDIO_ASR_PROVIDER=mock
AUDIO_MOCK_TEXT=Петя, сделай оплату к четвергу
AUDIO_WORKER_SAVE_CHUNKS=false
AUDIO_WORKER_CHUNKS_DIR=/tmp/grey-cardinal-audio-chunks
AUDIO_FASTER_WHISPER_MODEL=base

# Real STT service (asr-service / faster-whisper). Model is baked at build time.
# tiny|base|small|medium|large-v3 — larger = more accurate but slower/heavier.
WHISPER_MODEL=base
WHISPER_LANGUAGE=ru

FRONTEND_DASHBOARD_PORT=5173
# In the prod stack the dashboard is served same-origin behind Caddy, so these
# are derived from the page URL at runtime and can stay empty. Only set them for
# local `vite dev` against a backend on another host.
VITE_API_BASE_URL=
VITE_BRAIN_WS_URL=

LOG_LEVEL=INFO
