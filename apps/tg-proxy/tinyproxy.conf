User tinyproxy
Group tinyproxy
Port 8888
Timeout 600

# Only reachable on the internal docker network (port is not published).
Allow 0.0.0.0/0

# Allow HTTPS CONNECT tunneling (Telegram Bot API is https://api.telegram.org).
ConnectPort 443
ConnectPort 563

MaxClients 50
LogLevel Warning
