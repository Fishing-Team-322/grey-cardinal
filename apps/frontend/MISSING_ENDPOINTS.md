# Missing backend endpoints

No blocking frontend endpoints are currently missing.

Future product enhancements can add:

- CI provider details (commit SHA, workflow URL and rollback action) to `/api/deploy/status`.
- User-managed Yandex OAuth credentials if Telemost moves from managed mode to per-team mode.
