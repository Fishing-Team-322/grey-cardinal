server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Hash-routed SPA: always serve index.html for non-asset paths.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
